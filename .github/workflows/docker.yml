name: Build and Push Docker Image

on:
  push:
    branches: [ "refactor" ]

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build user-service
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/user-service/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}-user-service:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository }}-user-service:latest

      - name: Build auth-service
        uses: docker/build-push-action@v6
        with:
          context: ./services/auth-service
          file: Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}-auth-service:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository }}-auth-service:latest

      - name: Build static-server
        uses: docker/build-push-action@v6
        with:
          context: ./static-server
          file: Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}-static-server:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository }}-static-server:latest

      - name: Build nginx
        uses: docker/build-push-action@v6
        with:
          context: ./infra/nginx
          file: Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}-nginx:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository }}-nginx:latest

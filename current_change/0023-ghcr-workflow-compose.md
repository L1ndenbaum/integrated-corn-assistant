# GHCR workflow + compose images

- Rewrite GitHub Actions workflow to build/push four images with repo-based tags.
- Update docker-compose to use GHCR images (no local build) aligned with workflow tags.

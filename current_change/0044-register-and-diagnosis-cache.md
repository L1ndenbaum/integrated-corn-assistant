# Register API + diagnosis image cache

- Update register page to use /api/v1/user/register and remove legacy user-info fetch.
- Improve diagnosis-service Dockerfile caching (pip cache + model download cache).

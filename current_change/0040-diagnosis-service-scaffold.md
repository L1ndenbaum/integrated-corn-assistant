# Diagnosis-service scaffold

- Move diagnosis FastAPI service into services/diagnosis-service and update API path to /api/v1/diagnosis.
- Add Dockerfile and .dockerignore for diagnosis-service with BuildKit pip cache.
- Add diagnosis-service image build in GitHub Actions with GHA cache scope.
- Update frontend diagnosis API path and docs layout.

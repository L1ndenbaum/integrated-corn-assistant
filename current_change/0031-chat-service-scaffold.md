# Chat-service scaffold

- Split legacy ChatInterface into a standalone chat-service with config, handlers, and Dockerfile.
- Add chat-service to api-gateway proxy, docker-compose, and GitHub Actions build.
- Add chat-service envs to .env.example and deploy/docker/README.md.
